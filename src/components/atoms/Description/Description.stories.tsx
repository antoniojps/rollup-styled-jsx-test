import React, { ReactElement } from 'react';
import Description from './Description';

export default {
  title: 'atoms/Description',
};

export const Basic = (): ReactElement => <Description>Description</Description>;
