import React, { ReactElement } from 'react';
import DividerVertical from './DividerVertical';

export default {
  title: 'atoms/DividerVertical',
};

export const Basic = (): ReactElement => <DividerVertical />;
